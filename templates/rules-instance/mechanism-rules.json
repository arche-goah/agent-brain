[
  {
    "_comment": "Rules for core/helpers/mechanism-guard.cjs — instance knowledge. pattern = JS regex matched against the command of the matched tool. That is Bash AND Monitor: a hand-built watcher is a Monitor command, and the guard reads tool_input.command without ever looking at tool_name. Every newly discovered shortcut gets added here. Example rule (generic, fine to keep):",
    "pattern": "\\bnetworksetup\\s+-set|\\bifconfig\\s+\\S+\\s+(inet|alias)\\b|\\broute\\s+(add|delete)\\b|\\bipconfig\\s+set\\b",
    "was": "IP/routing configuration done locally on the end device",
    "stattdessen": "Addresses come from the network setup (DHCP reservation on the router or similar) — use the instance's documented path; record here where it lives."
  },
  {
    "_comment": "Example rule 2 (generic, fine to keep): waiting for CI with a hand-built poll loop. scripts/ci-watch.sh enumerates every terminal state and knows that `gh pr checks` exits 8 while checks are still pending; an ad-hoc loop reads that exit as an error, swallows it and ends silently, which looks exactly like still running. The negative lookahead lets a command through that calls ci-watch.sh itself, even in a compound with sleep.",
    "pattern": "^(?![\\s\\S]*ci-watch\\.sh)[\\s\\S]*?(?:while\\s+true|until\\s|for\\s*\\(\\(|\\bsleep\\s+\\d+)[\\s\\S]*\\bgh\\s+(?:pr\\s+checks|run\\s+(?:list|view|watch))\\b",
    "was": "waiting for CI with a hand-built poll loop",
    "stattdessen": "scripts/ci-watch.sh pr <owner/repo> <nr> (or ref <owner/repo> <ref>), started via Monitor. Exit 0 = all green, 1 = something failed in CI, 2 = could NOT be measured - 2 is loud on purpose and is not green."
  }
]
